import 'package:flutter/material.dart';

class ImageLabelerScreen extends StatelessWidget {
  const ImageLabelerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
